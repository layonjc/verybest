<!-- Validation failure messages -->
<% if @bucket_list.errors.any? %>
  <% @bucket_list.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Bucket List #<%= @bucket_list.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_bucket_list/<%= @bucket_list.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <button class="btn btn-warning">
        Update Bucket List
      </button>
      or
      <a href="/bucket_lists/<%= @bucket_list.id %>">Cancel</a>
    </form>
  </div>
</div>
